import React from 'react';
import './Footer.css';

const Footer: React.FC = () => {
  return (
    <footer className="footer">
      <div className="footer-container">
        {/*
          Removed the footer content including all contact info and dividers.
          This leaves the footer container empty, so you can add new content here later if you want.
        */}
      </div>
    </footer>
  );
};

export default Footer;